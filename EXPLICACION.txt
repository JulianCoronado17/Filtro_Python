1. Estructura de modulacion

EN EL MAIN: 
    Menus
    Diccionarios de TODO:
    {
        campers=[]
        trainers=[]
        rutas=[]
        coordinadores=[]
    }

import os
def AddPrueba(camper):
#2.ingresar notas. Prueba es aprobada si el promedio entre la nota teórica y 
#la nota practica es >=60.    
    for i in camper:
        print(f'~~~ {i} ~~~ {camper[i]["nombre"]} {camper[i]["apellido"]} ~~~')
    id=str(input('Ingrese el ID del estudiante: \n'))
    practica=int(input('Ingrese la nota de la prueba practica del aspirante'))
    teorica=int(input('Ingrese la nota de la prueba teorica del aspirante'))
    #trabajos=int(input(f'Ingrese la nota de los trabajos de {camper ["id"]["nombre"]}'))

    notaFinal=(practica*0.60)+(teorica*0.30)
    if notaFinal>=60:
        camper [id]['estado']="aprobado"
        print(f'El aspirante ha sido aprobado')
        os.system('pause')
    else:
        print('No ha sido aprobado')
        os.system('pause')
